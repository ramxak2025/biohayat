/**
 * Единый источник параметров текущей акции. Используется и в hero-блоке
 * (как значения по умолчанию, если баннер не задан в админке), и в блоке
 * «Распродажа» — чтобы цифры и промокод не расходились между секциями.
 */
export const SALE = {
  /** Максимальная скидка на хиты, %. */
  maxDiscount: 40,
  /** Скидка на первый заказ, %. */
  firstOrderDiscount: 25,
  /** Промокод на первый заказ. */
  promoCode: "FREE25Hayat",
} as const;
